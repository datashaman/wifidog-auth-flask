from app.users.models import User, UserSchema
from app.vouchers.models import Voucher, VoucherSchema
from app.wifidog.models import Ping, Auth
from app.gateways.models import Gateway, GatewaySchema
from app.networks.models import Network, NetworkSchema
